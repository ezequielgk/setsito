/**
 * POOL - Configuración Global
 * Constantes y configuración del slider
 */

const CONFIG = {
    slideInterval: 4000,         // Tiempo entre slides (ms)
    transitionDuration: 800,     // Duración de transición (ms)
    progressUpdateInterval: 50,  // Actualización de barra de progreso (ms)
    swipeThreshold: 50           // Umbral mínimo para detectar swipe (px)
};
